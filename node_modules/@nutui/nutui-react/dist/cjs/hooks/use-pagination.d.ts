interface PaginationOptions {
    current: number;
    total: number;
    itemsPerPage: number;
    displayCount: number;
    ellipse: boolean;
}
export type PaginationNode = {
    number: number;
    text: string;
    selected?: boolean;
};
type PaginationResult = [
    PaginationNodes: Array<PaginationNode>,
    PaginationNodesCount: number
];
export declare const usePagination: (options: PaginationOptions) => PaginationResult;
export {};
