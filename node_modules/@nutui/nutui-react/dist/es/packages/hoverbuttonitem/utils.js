import React from "react";
export var getIcon = function(icon) {
    return React.isValidElement(icon) ? React.cloneElement(icon, {
        // @ts-ignore
        className: 'nut-icon'
    }) : icon;
};
