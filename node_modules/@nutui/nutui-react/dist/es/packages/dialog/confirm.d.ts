import { BaseDialog, DialogReturnProps } from "../../types";
type DialogConfirmNativeProps = Partial<BaseDialog>;
declare const confirm: (config: DialogConfirmNativeProps, renderFunc?: (props: DialogConfirmNativeProps) => void) => DialogReturnProps;
export default confirm;
