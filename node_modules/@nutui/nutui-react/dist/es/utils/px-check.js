export var pxCheck = function(value) {
    // if (!Number.isNaN(Number(value))) return Number(value)
    // return Number.isNaN(Number(value)) ? String(value) : `${value}px`
    return parseFloat(value.toString());
};
